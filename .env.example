# Streamlit UI -> FastAPI prediction service
PREDICT_API_BASE=http://127.0.0.1:8000

# Weather source is Open-Meteo (no API key required).
# Optional: enable richer multilingual summaries via Groq.
GROQ_API_KEY=your_groq_key_here
